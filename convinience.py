# Map: List : common functionality on all the items.
# Filter: List - Selected values after applyint transformation.
# Reduce: Single value: After applying some operation on all the items.
# def sqr(x):
# return x*x
# map(sqr, items)
# map(lambda x:x*x, items)
# filter(lambda x:x<3, items)
# reduce(lambda x,y:x+y, items)
#
# Comprehended list or Genrators:
# [x for x in range(10) if(x%2)] = [1,3,5,7,9]
#
# Iterator:
# [1,3,5,7,9]
#
# Decorator:
# Wrapping the common functionality to make it generic or specific.
#
# Alias
# lst1 = lst2
# print id(lst1)
# print id(lst2)
#